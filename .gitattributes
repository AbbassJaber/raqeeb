# Shell scripts and the Dockerfile MUST use LF line endings, or they break when run in a
# Linux container (`sh` treats a trailing \r as part of the command -> "not found").
*.sh        text eol=lf
Dockerfile  text eol=lf
