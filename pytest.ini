[pytest]
# Unit tests live in tests/. scripts/ holds operational scripts (e.g. smoke_test.py,
# a *live* HTTP check that requires a running server) — not pytest tests, so keep them
# out of collection. Run the live smoke test explicitly against a running server.
testpaths = tests
